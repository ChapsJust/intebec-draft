# Drizzle
DATABASE_URL="postgres://root:mysecretpassword@localhost:5432/local"

# IA locale (Ollama). Optionnel : sans ces variables, la génération déterministe du document
# fonctionne normalement, seule la rédaction assistée est indisponible.
# Sur l'infrastructure Intébec, pointer vers le Mac Studio joint par Tailscale.
OLLAMA_URL="http://localhost:11434"
OLLAMA_MODEL="llama3.1:8b"
